kind: Cluster
apiVersion: kind.x-k8s.io/v1alpha4
name: mlzoomcamp
nodes:
  - role: control-plane
    extraPortMappings:
      # Expose the NodePort service on the host (WSL2-friendly)
      - containerPort: 30080
        hostPort: 30080
        protocol: TCP
