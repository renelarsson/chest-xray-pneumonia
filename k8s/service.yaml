apiVersion: v1
kind: Service
metadata:
  name: chest-xray-pneumonia
spec:
  selector:
    app: chest-xray-pneumonia
  ports:
    - name: http
      port: 9696
      targetPort: 9696
      protocol: TCP
      nodePort: 30080
  type: NodePort
